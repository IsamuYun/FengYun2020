#include <ansi.h>
inherit ITEM;
void create()
{	set_name("八仙桌",({"table"}));
	set("long","一张结实的八仙桌，摆在堂屋，厨房，厢房都不错。\n");
	set("unit","张");
	set("value",100);
	set("no_get",1);
     set("is_jiaju",1);
}
